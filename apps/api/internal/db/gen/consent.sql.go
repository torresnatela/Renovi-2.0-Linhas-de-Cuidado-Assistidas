// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: consent.sql

package gen

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const getActiveConsent = `-- name: GetActiveConsent :one
SELECT id, patient_id, gestao_contract_id, finalidade, versao_termo, status, concedido_em, revogado_em, created_at FROM consent
WHERE patient_id = $1 AND finalidade = $2 AND status = 'ativo'
`

type GetActiveConsentParams struct {
	PatientID  uuid.UUID `json:"patient_id"`
	Finalidade string    `json:"finalidade"`
}

func (q *Queries) GetActiveConsent(ctx context.Context, arg GetActiveConsentParams) (Consent, error) {
	row := q.db.QueryRow(ctx, getActiveConsent, arg.PatientID, arg.Finalidade)
	var i Consent
	err := row.Scan(
		&i.ID,
		&i.PatientID,
		&i.GestaoContractID,
		&i.Finalidade,
		&i.VersaoTermo,
		&i.Status,
		&i.ConcedidoEm,
		&i.RevogadoEm,
		&i.CreatedAt,
	)
	return i, err
}

const insertConsent = `-- name: InsertConsent :one
INSERT INTO consent (id, patient_id, gestao_contract_id, finalidade, versao_termo)
VALUES ($1, $2, $3, $4, $5)
RETURNING id, patient_id, gestao_contract_id, finalidade, versao_termo, status, concedido_em, revogado_em, created_at
`

type InsertConsentParams struct {
	ID               uuid.UUID   `json:"id"`
	PatientID        uuid.UUID   `json:"patient_id"`
	GestaoContractID pgtype.Text `json:"gestao_contract_id"`
	Finalidade       string      `json:"finalidade"`
	VersaoTermo      string      `json:"versao_termo"`
}

func (q *Queries) InsertConsent(ctx context.Context, arg InsertConsentParams) (Consent, error) {
	row := q.db.QueryRow(ctx, insertConsent,
		arg.ID,
		arg.PatientID,
		arg.GestaoContractID,
		arg.Finalidade,
		arg.VersaoTermo,
	)
	var i Consent
	err := row.Scan(
		&i.ID,
		&i.PatientID,
		&i.GestaoContractID,
		&i.Finalidade,
		&i.VersaoTermo,
		&i.Status,
		&i.ConcedidoEm,
		&i.RevogadoEm,
		&i.CreatedAt,
	)
	return i, err
}

const revokeActiveConsent = `-- name: RevokeActiveConsent :execrows
UPDATE consent
SET status = 'revogado', revogado_em = $3
WHERE patient_id = $1 AND finalidade = $2 AND status = 'ativo'
`

type RevokeActiveConsentParams struct {
	PatientID  uuid.UUID          `json:"patient_id"`
	Finalidade string             `json:"finalidade"`
	RevogadoEm pgtype.Timestamptz `json:"revogado_em"`
}

func (q *Queries) RevokeActiveConsent(ctx context.Context, arg RevokeActiveConsentParams) (int64, error) {
	result, err := q.db.Exec(ctx, revokeActiveConsent, arg.PatientID, arg.Finalidade, arg.RevogadoEm)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
