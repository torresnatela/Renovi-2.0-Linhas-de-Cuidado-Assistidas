# Caddyfile de PRODUÇÃO (referência). TLS automático + proxy reverso.
# Ajuste o domínio após a decisão do §10.5 do SPEC. Não é usado no dev local
# (onde a API sobe via `go run` e o web via Vite).
#
# Topologia: Caddy -> Web SPA (estático) + /api -> API Go.

# renovi.example.com {
#     encode zstd gzip
#
#     # API (a porta deve casar com RENOVI_HTTP_ADDR — default :8090)
#     handle /api/* {
#         reverse_proxy api:8090
#     }
#
#     # SPA (arquivos estáticos do build do front)
#     handle {
#         root * /srv/web
#         try_files {path} /index.html
#         file_server
#     }
# }

# Placeholder local (evita erro se alguém subir o Caddy sem domínio configurado).
:80 {
	respond "Renovi 2.0 — configure o domínio no Caddyfile (SPEC §10.5)." 200
}
